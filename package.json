{
  "name": "cc-mini-project-mini-raft",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "dev:gateway": "npm run dev -w @mini-raft/gateway",
    "dev:replica": "npm run dev -w @mini-raft/replica",
    "build": "npm run build --workspaces",
    "typecheck": "npm run typecheck --workspaces"
  }
}
